<!DOCTYPE html>
<html lang="hi">
<head>
    <meta charset="UTF-8">
    <title>Admin Login | BSC</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-blue-900 h-screen flex items-center justify-center">
    <div class="bg-white p-8 rounded-xl shadow-2xl w-full max-w-sm">
        <h1 class="text-2xl font-bold text-center text-blue-900 mb-6 italic underline uppercase">Admin Login</h1>
        <form id="loginForm" class="space-y-4">
            <div>
                <label class="block text-sm font-bold mb-1 italic">Email</label>
                <input type="email" id="email" class="w-full border p-2 rounded outline-none" required placeholder="admin@bscollege.com">
            </div>
            <div>
                <label class="block text-sm font-bold mb-1 italic">Password</label>
                <input type="password" id="password" class="w-full border p-2 rounded outline-none" required placeholder="danapur123">
            </div>
            <button type="submit" class="w-full bg-blue-900 text-white py-2 rounded font-bold hover:bg-blue-800 transition">LOG IN</button>
        </form>
        <p id="msg" class="text-red-500 text-sm mt-4 text-center hidden font-bold">Wrong password!</p>
        <a href="index.html" class="block text-center mt-4 text-gray-400 text-xs">← Back to Site</a>
    </div>

    <script>
        document.getElementById('loginForm').addEventListener('submit', (e) => {
            e.preventDefault();
            const email = document.getElementById('email').value;
            const pass = document.getElementById('password').value;
            if(email === "admin@bscollege.com" && pass === "danapur123") {
                window.location.href = "admin.html";
            } else {
                document.getElementById('msg').classList.remove('hidden');
            }
        });
    </script>
</body>
</html>